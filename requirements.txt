# LocalMuse AI — root dependencies (Python 3.11)
# FE/BE 공통 설치: pip install -r requirements.txt

streamlit>=1.32.0,<2
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0
pandas>=2.0.0

# Test
pytest>=8.0.0

# DB — NCP Cloud DB(MySQL). 로컬에서 실패 시 SQLite 폴백 (DB_FALLBACK_SQLITE=true)
pymysql>=1.1.0
